---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Terminal from "../components/Terminal.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";
---
<!doctype html>
<html lang="es">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
    </head>
    <body>
        <Header />
        <main>
            <Terminal>
                <span class="terminal-line delay-0 input">curl -i this-page</span>
                <span class="terminal-line delay-3500 output">NOT FOUND</span>
                <span class="terminal-line delay-000 input">curl -i WTF??????</span>
                <span class="terminal-line delay-3500 output">NOT FOUND</span>
                <span class="terminal-line delay-000 input">sudo rm -dfr /</span>
                <span class="terminal-line delay-0 input">curl -i this-page</span>
                <span class="terminal-line delay-3500 output">NOT FOUND</span>
                <span class="terminal-line delay-000 input">curl -i WTF??????</span>
                <span class="terminal-line delay-3500 output">NOT FOUND</span>
                <span class="terminal-line delay-000 input">sudo rm -dfr /</span>
                <span class="terminal-line delay-0 input">curl -i this-page</span>
                <span class="terminal-line delay-3500 output">NOT FOUND</span>
                <span class="terminal-line delay-000 input">curl -i WTF??????</span>
                <span class="terminal-line delay-3500 output">NOT FOUND</span>
                <span class="terminal-line delay-000 input">sudo rm -dfr /</span>
                <span class="terminal-line delay-0 input">curl -i this-page</span>
                <span class="terminal-line delay-3500 output">NOT FOUND</span>
                <span class="terminal-line delay-000 input">curl -i WTF??????</span>
                <span class="terminal-line delay-3500 output">NOT FOUND</span>
                <span class="terminal-line delay-000 input">sudo rm -dfr /</span>
            </Terminal>
        </main>
    </body>
</html>